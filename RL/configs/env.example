# Copy to .env or source selectively before running.
# Keep all paths relative to the repository root unless your infra layer injects them.

ACE_SQL_CONDA_ENV=ace-sql
ACE_SQL_MODEL_PATH=models/sft_checkpoint

ACE_SQL_TRAIN_DB_ROOT=external/train_databases
ACE_SQL_DEV_DB_ROOT=external/dev_databases
ACE_SQL_LOOSE_DB_ROOTS=external/databases

ACE_SQL_WANDB_MODE=offline
ACE_SQL_EXPERIMENT_NAME=rl_pcgrad

CUDA_VISIBLE_DEVICES=0,1,2,3
